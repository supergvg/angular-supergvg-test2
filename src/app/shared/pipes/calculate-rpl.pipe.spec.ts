import { CalculateRplPipe } from './calculate-rpl.pipe';

describe('CalculateRplPipe', () => {
	it('create an instance', () => {
		const pipe = new CalculateRplPipe();
		expect(pipe).toBeTruthy();
	});
});
